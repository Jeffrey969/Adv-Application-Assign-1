# Program Name: Assignment1
# Course: IT3883/Section W01
# Student Name: Jeffrey Brown
# Assignment Number: Lab 1
# Due Date: 09/05/2025
# Purpose: This program creates a text-based menu with four options.
#          The user can append text to a buffer, clear the buffer,
#          display the buffer, or exit the program.
# Resources: class notes, practice.

# initialize the buffer
buffer = ""

# main loop for menu
while True:
    # display menu
    print("Menu")
    print("1. Append data to the input buffer")
    print("2. Clear the input buffer")
    print("3. Display the input buffer")
    print("4. Exit the program")

    # get user choice
    choice = input("Enter your choice!: ")

    # 1: append data
    if choice == "1":
        new_data = input("Enter text to append: ")
        buffer += new_data  # add new data
        print("Data appended.")

    # 2: clear
    elif choice == "2":
        buffer = ""  # reset
        print("Buffer cleared.")

    # 3: display
    elif choice == "3":
        if buffer:  # If buffer is not empty
            print("Current buffer contents:")
            print(buffer)
        else:
            print("Buffer is empty.")

    # 4: exit program
    elif choice == "4":
        print("Exiting program. Goodbye!")
        break  # exit

    # Handle invalid input
    else:
        print("Invalid, Please enter a number from 1 to 4.")
